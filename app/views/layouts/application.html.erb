<!DOCTYPE html>
<html>
  <head>
    <title>Baseball Team</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400,700,900&display=swap" rel="stylesheet">
  </head>

  <body>
    <header class="header">
      <div class="inner">
        <div class="nav">
          <div class="nav__left">
            <%= link_to image_tag("logo.png", class: :logo), root_path %>
          </div>
          <% if user_signed_in? %>
            <div class="nav__right">
              <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete }, class: :nav__btn %>
              <%= link_to "スケジュール投稿", new_schedule_path, class: :nav__btn %>
              <%= link_to "選手紹介", user_path(:id), class: :nav__btn %>
              <%= link_to "過去の対戦", users_path, class: :nav__btn %>
            </div>
          <% else %>
            <div class="nav__right">
              <%= link_to "ログイン", new_user_session_path, class: :nav__btn %>
              <%= link_to "新規登録", new_user_registration_path, class: :nav__btn %>
            </div>
          <% end %>
        </div>
      </div>
        <div class="greeting">
          <% if user_signed_in? %>
          <div class="greeting__link">
            <%= "☆ ようこそ、#{current_user.name_kanji} さん ☆" %>
          </div>
          <div class="greeting__today">
            <%= "【本日：#{Date.today}】" %>
          </div>
          <% end %>
        </div>
    </header>
    <%= yield %>
    <footer class="footer">
    </footer>
  </body>
</html>
